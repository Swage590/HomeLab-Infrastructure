version: '3.9'

services:
  nginx-lb:
    image: nginx:latest
    container_name: nginx-lb
    ports:
      - "6443:6443"
    volumes:
      - /home/swage/nginx-lb/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped

services:
  nginx-lb:
    image: nginx:latest
    container_name: nginx-lb
    network_mode: host
    volumes:
      - /home/swage/nginx-lb/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
